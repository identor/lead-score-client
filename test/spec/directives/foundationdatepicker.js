'use strict';

describe('Directive: foundationDatePicker', function () {

  // load the directive's module
  beforeEach(module('leadScoreClientApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));
});
